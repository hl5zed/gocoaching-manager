# GOThriveCoaching 운영 전 최종 점검 체크리스트

## 로그인/권한
- [ ] 코치메이커 계정으로 로그인된다.
- [ ] 일반 코치 또는 코칭 대상자 계정은 `/coach-maker` 접근이 제한된다.
- [ ] 비활성화된 계정은 대시보드 접근이 제한된다.
- [ ] 권한 오류 화면에 raw DB 오류, 토큰, auth metadata가 보이지 않는다.
- [ ] 문제가 있으면 `profiles`, `user_roles`, 계정 `status`를 먼저 확인한다.

## 코치메이커 대시보드
- [ ] `/coach-maker` 페이지가 정상 렌더링된다.
- [ ] 목실기 요약 카드가 표시된다.
- [ ] 관심 필요 대상자 요약과 상위 5명 목록이 표시된다.
- [ ] `전체 목실기 현황 보기` 버튼이 `/coach-maker/moksilgi-progress`로 이동한다.
- [ ] `인쇄용 보고서 보기` 버튼이 `/coach-maker/report`로 이동한다.
- [ ] 빈 데이터 상태에서도 화면이 깨지지 않는다.

## 목실기 현황
- [ ] `/coach-maker/moksilgi-progress` 페이지가 정상 렌더링된다.
- [ ] 검색, 지역 필터, 팀 필터가 정상 작동한다.
- [ ] 정렬과 진행률 배지가 정상 표시된다.
- [ ] 상세보기 패널이 열리고 닫힌다.
- [ ] 월별 진행 상태가 1월부터 12월까지 표시된다.
- [ ] 필터 결과가 없을 때 빈 상태 메시지가 표시된다.
- [ ] 문제가 있으면 선택 연도, 팀명, `moksilgi_monthly_summaries` 데이터를 확인한다.

## 관리 액션 메모
- [ ] 관리 액션 메모 목록이 조회된다.
- [ ] 새 메모가 저장되고 새로고침 후에도 남아 있다.
- [ ] 상세보기와 수정 저장이 작동한다.
- [ ] 완료 처리와 상태 빠른 변경이 작동한다.
- [ ] 우선순위 빠른 변경이 작동한다.
- [ ] 목록에서 제거가 soft delete로 처리되어 목록에서 사라진다.
- [ ] 검색, 필터, 정렬, 페이지네이션이 함께 작동한다.
- [ ] 요약 카드 드릴다운과 드릴다운 해제가 정상 작동한다.
- [ ] 문제가 있으면 `coach_action_notes.created_by`가 `profiles.id` 기준인지 확인한다.

## CSV 내보내기
- [ ] 관리 액션 메모 CSV 내보내기가 현재 필터 결과 기준으로 생성된다.
- [ ] 목실기 현황 CSV 내보내기가 현재 검색/필터/정렬 결과 기준으로 생성된다.
- [ ] CSV 파일명이 날짜를 포함한다.
- [ ] 한글이 깨지지 않는다.
- [ ] 쉼표, 줄바꿈, 따옴표가 포함된 값도 깨지지 않는다.
- [ ] 내보낼 데이터가 없을 때 안내 메시지가 표시된다.

## 인쇄용 보고서/PDF 출력
- [ ] `/coach-maker/report` 페이지가 정상 렌더링된다.
- [ ] `인쇄하기` 버튼이 브라우저 인쇄 창을 연다.
- [ ] 인쇄/PDF 저장 시 버튼과 내비게이션이 숨겨진다.
- [ ] 보고서 제목, 생성일, 기준 연도, 설명이 출력된다.
- [ ] 목실기 성취 현황, 관심 필요 대상자, 관리 액션 메모 요약, 우선 조치 목록이 출력된다.
- [ ] 섹션들이 각각 새 A4 페이지로 강제 분리되지 않고 자연스럽게 이어진다.
- [ ] 긴 메모와 긴 텍스트가 잘리지 않고 줄바꿈된다.

## 보고서 필터/자동 요약
- [ ] `year` query로 연도별 보고서가 표시된다.
- [ ] `team` query로 팀별 보고서가 표시된다.
- [ ] `from`, `to` query로 기간별 보고서가 표시된다.
- [ ] 필터 입력 UI는 화면에 보이고 인쇄 시 숨겨진다.
- [ ] 적용된 필터 요약은 화면과 인쇄물에 포함된다.
- [ ] 보고서 상단 자동 요약 문장이 현재 필터 조건을 반영한다.
- [ ] 데이터가 없으면 `선택한 조건에 해당하는 보고서 데이터가 없습니다.`가 표시된다.
- [ ] 화면에 `NaN`, `Infinity`, `undefined`, `null`이 보이지 않는다.
